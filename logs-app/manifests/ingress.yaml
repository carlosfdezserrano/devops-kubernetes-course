apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: logs-app-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: logs-app-svc
            port:
              number: 2345
